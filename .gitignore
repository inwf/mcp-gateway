# ===== Legacy TypeScript implementation (reference only, not tracked) =====
old-legacy/
REFACTOR_PLAN.md

# ===== Claude Code local state =====
.claude/
MEMORY.md

# ===== Runtime data (config, logs, pid) — always project-local, never ~/ =====
backend/data/
data/

# ===== Docker =====
# The compose file people actually run. docker-compose.example.yml is the
# tracked template; this is the copy, and two of its settings — the
# published port and the network's subnet — depend on what else is running
# on the machine. Tracking it would mean anyone who changed one of them had
# a dirty tree and a change to offer that nobody else wants.
docker-compose.yml
docker-compose.override.yml

# ===== Go =====
backend/bin/
backend/mcphub
*.test
*.out
coverage.out
coverage.html

# Embedded frontend build artifacts, generated at build time
backend/internal/webui/dist/

# ===== Node / frontend =====
node_modules/
frontend/dist/
frontend/.vite/
frontend/coverage/
*.tsbuildinfo
npm-debug.log*
yarn-error.log*
pnpm-debug.log*

# ===== Editors / OS =====
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# ===== Env =====
.env
.env.local
.env.*.local
